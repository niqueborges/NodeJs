const {Person} = require('./person'); // Import the Person class from person

const person = new Person('Alice'); // Create a new instance of Person

console.log(person.sayMyName()); // My name is Alice!
